export namespace EnumUtil {
    export const enum Stage {
        DEVELOPMENT = 'dev',
        QUALITY = 'qas',
        HOMOLOGACAO = 'hmg',
        PRODUCTION = 'prd'
    }

    export const enum LogLevel {
        ERROR = 'ERROR',
        WARN = 'WARN',
        INFO = 'INFORMATION',
        DEBUG = 'DEBUGGIN'
    }
}
